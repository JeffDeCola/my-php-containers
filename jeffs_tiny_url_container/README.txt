The trick is in the .htaccess file.

You place in the folder you want the url to be and point to the php script.

RewriteRule ^([\w\d]{4})$ XXXXXXXXXXXXXXXXXXXXX?decode=$1 [L]

You can move around this location anytime you want.

Simple Update the links in the code in 
jeffs_tiny_url_list_of_urls.php and

HOW TO CREATE A SQL DATABASE


